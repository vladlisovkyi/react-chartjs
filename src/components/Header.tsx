import React from "react";

const Header = () => {
  return (
    <header className="w-full h-20 px-5 flex items-center justify-center bg-white sticky top-0 z-10">
      <span className="text-2xl font-semibold">Chart.js + React</span>
    </header>
  );
};

export default Header;
